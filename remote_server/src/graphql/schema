var graphqlHTTP = require('express-graphql')
var { buildSchema } = require('graphql')

var schema = buildSchema(`
    type Query {
        hello: String
    }
`)

// Provide resolver functions for each API endpoint
var root = {
    hello: () => {
        return 'Hello World!'
    }
}

module.exports = root
